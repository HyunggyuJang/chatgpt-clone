version: "3.4"

services:
  api:
    ports:
      - 3080:3080
    depends_on:
      - mongodb
    image: node-api
    build:
      context: .
      target: node-api
    restart: always
    env_file:
      - ./api/.env
    environment:
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongodb:27017/chatgpt-clone
    volumes:
      - /dev:/dev
      - ./api:/api
      - /api/node_modules
  mongodb:
    image: mongo:4.4.18
    restart: always
    container_name: mongodb
    volumes:
      - /dev:/dev
      - ./data-node:/data/db
    command: mongod --noauth
